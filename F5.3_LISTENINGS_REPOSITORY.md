# F5.3 — ListeningsRepository

Se extrae el SQL de Escuchas hacia:

`src/modules/listenings/listenings.repository.js`

Incluye también el bloque final:

`GET /api/escuchas/lotes/:id/tickets`

Arquitectura temporal:

```text
server.js handlers
   ↓
ListeningsRepository
   ↓
PostgreSQL
```

Permanece en `server.js`:

- tokens;
- lectura de body;
- validaciones;
- duplicados/contadores;
- mensajes;
- status HTTP;
- rutas.

`server.js`: 4307 → 4189 líneas.

Próxima fase: F5.4 — ListeningsService.
