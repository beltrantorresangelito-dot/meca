# MECA F11.5.6 - Inventario de campana

Referencias: **991**
Archivos: **75**

## Resumen

- DIAGNOSTICO: 39
- ID_DOMINIO: 545
- REVISAR: 4
- SQL: 95
- TEST: 22
- TEXTO_LEGACY_O_UI: 219
- UI: 67

## Referencias de campana textual asociadas a evaluaciones

- `F11.4_FLUJO_CONTEXTO_MULTQUIEBRE.json:655` — `"text": "     1: const DomainService = require('./domain.service');\n     2: const DomainController = require('./domain.controller');\n     3: \n     4: function registerDomainRoutes(routes, { service = new DomainService() } = {}) {\n     5:   if (!routes || typeof routes !== 'object') {\n     6:     throw new Error('routes es obligatorio');\n     7:   }\n     8: \n     9:   const controller = new DomainController(service);\n    10: \n    11:   const definitions = [\n    12:     ['/api/domain/quiebres', controller.listBreaks.bind(controller)],\n    13:     ['/api/domain/campanas', controller.listCampaigns.bind(controller)],\n    14:     ['/api/domain/matrices', controller.listMatrices.bind(controller)],\n    15:     ['/api/domain/campana-matriz', controller.campaignMatrixHistory.bind(controller)],\n    16:     ['/api/domain/contexto-evaluacion', controller.resolveContext.bind(controller)],\n    17:     ['/api/domain/consistencia', controller.consistency.bind(controller)]\n    18:   ];\n    19: \n    20:   for (const [path, handler] of definitions) {\n    21:     routes[path] = routes[path] || {};\n    22:     routes[path].GET = handler;\n    23:   }\n    24: \n    25:   return definitions.map(([path]) => path);\n    26: }\n    27: "`
- `F11.4_FLUJO_CONTEXTO_MULTQUIEBRE.json:666` — `"text": "     1: const DomainService = require('./domain.service');\n     2: const DomainController = require('./domain.controller');\n     3: \n     4: function registerDomainRoutes(routes, { service = new DomainService() } = {}) {\n     5:   if (!routes || typeof routes !== 'object') {\n     6:     throw new Error('routes es obligatorio');\n     7:   }\n     8: \n     9:   const controller = new DomainController(service);\n    10: \n    11:   const definitions = [\n    12:     ['/api/domain/quiebres', controller.listBreaks.bind(controller)],\n    13:     ['/api/domain/campanas', controller.listCampaigns.bind(controller)],\n    14:     ['/api/domain/matrices', controller.listMatrices.bind(controller)],\n    15:     ['/api/domain/campana-matriz', controller.campaignMatrixHistory.bind(controller)],\n    16:     ['/api/domain/contexto-evaluacion', controller.resolveContext.bind(controller)],\n    17:     ['/api/domain/consistencia', controller.consistency.bind(controller)]\n    18:   ];\n    19: \n    20:   for (const [path, handler] of definitions) {\n    21:     routes[path] = routes[path] || {};\n    22:     routes[path].GET = handler;\n    23:   }\n    24: \n    25:   return definitions.map(([path]) => path);\n    26: }\n    27: \n    28: module.exports = { registerDomainRoutes };"`
- `F11.4_FLUJO_CONTEXTO_MULTQUIEBRE.json:677` — `"text": "     1: const DomainService = require('./domain.service');\n     2: const DomainController = require('./domain.controller');\n     3: \n     4: function registerDomainRoutes(routes, { service = new DomainService() } = {}) {\n     5:   if (!routes || typeof routes !== 'object') {\n     6:     throw new Error('routes es obligatorio');\n     7:   }\n     8: \n     9:   const controller = new DomainController(service);\n    10: \n    11:   const definitions = [\n    12:     ['/api/domain/quiebres', controller.listBreaks.bind(controller)],\n    13:     ['/api/domain/campanas', controller.listCampaigns.bind(controller)],\n    14:     ['/api/domain/matrices', controller.listMatrices.bind(controller)],\n    15:     ['/api/domain/campana-matriz', controller.campaignMatrixHistory.bind(controller)],\n    16:     ['/api/domain/contexto-evaluacion', controller.resolveContext.bind(controller)],\n    17:     ['/api/domain/consistencia', controller.consistency.bind(controller)]\n    18:   ];\n    19: \n    20:   for (const [path, handler] of definitions) {\n    21:     routes[path] = routes[path] || {};\n    22:     routes[path].GET = handler;\n    23:   }\n    24: \n    25:   return definitions.map(([path]) => path);\n    26: }\n    27: \n    28: module.exports = { registerDomainRoutes };\n    29: "`
- `F11.4_FLUJO_CONTEXTO_MULTQUIEBRE.txt:93` — `7418:         campanaInput.value = evaluacion.campana || 'Sin campaña';`
- `F11.4_FLUJO_CONTEXTO_MULTQUIEBRE.txt:190` — `2406:         const conCampana = evaluacionesFormateadas.filter(e => e.campana && e.campana !== '').length;`
- `F11.4_FLUJO_CONTEXTO_MULTQUIEBRE.txt:201` — `2406:         const conCampana = evaluacionesFormateadas.filter(e => e.campana && e.campana !== '').length;`
- `F11.4_FLUJO_CONTEXTO_MULTQUIEBRE.txt:210` — `2415:                 campana: evaluacionesFormateadas[0]?.campana,`
- `public/js/auditor.js:7816` — `campanaInput.value = evaluacion.campana || 'Sin campaña';`
- `public/js/supervisor.js:2406` — `const conCampana = evaluacionesFormateadas.filter(e => e.campana && e.campana !== '').length;`
- `public/js/supervisor.js:2415` — `campana: evaluacionesFormateadas[0]?.campana,`
- `public/js/supervisor.js:18607` — `// Para campaña, filtramos directamente por el campo campana en evaluaciones`
- `public/js/supervisor.js:18608` — `const evaluacionesFiltradasCampana = evaluacionesBase.filter(e => e.campana === valor);`
- `scripts/f1155-persistir-contexto-evaluacion.js:195` — `console.log('[F11.5.5] OK - campana/matriz/version obligatorias.');`
- `scripts/f1156-inventario-campana.js:111` — `md.push('## Referencias de campana textual asociadas a evaluaciones');`
- `tests/evaluation-campaign-resolution.f11553.test.js:41` — `'F11553-001 enriquecedor usa campana de evaluacion o escucha real',`

## Criterio de migración

- `campana_id`: fuente de verdad persistente.
- `campana`: solo debe sobrevivir como dato derivado para presentación.
- No eliminar columnas ni propiedades hasta revisar todas las referencias.
- Reportes y vistas deben resolver código/nombre mediante la relación con `campanas`.