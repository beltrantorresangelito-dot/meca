# F4.2 — AgentsRepository

## Objetivo

Extraer exclusivamente el acceso PostgreSQL del dominio Agentes.

Arquitectura temporal:

```text
server.js handlers
      ↓
AgentsRepository
      ↓
PostgreSQL
```

## Consultas movidas

- listado con filtros;
- actualización dinámica;
- eliminación;
- consulta por ID;
- categorías;
- listado completo;
- dataset de exportación CSV.

## Qué permanece en server.js

- validación local de token legacy;
- lectura de body;
- códigos 200/400/401/404/500;
- mensajes;
- construcción del CSV;
- Content-Type / Content-Disposition.

## Decisiones

No se cambia el comportamiento de `/api/agentes/completo`.
Actualmente no tiene chequeo local `Token requerido`; la validación Bearer
central sigue operando cuando se envía un token.

No se agrega Service ni Controller todavía.

## server.js

Antes: 4656 líneas
Después: 4574 líneas
Reducción: 82 líneas

## Validación

```powershell
npm test
```

Esperado: `0 fail`.

## Próxima fase

F4.3 — AgentsService:
validaciones, transformación y generación CSV fuera de `server.js`.
