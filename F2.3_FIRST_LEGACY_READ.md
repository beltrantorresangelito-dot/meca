# F2.3 — Primera lectura legacy migrada

Se migra solo `GET /api/matriz/versiones/activa`.

Antes: `server.js -> pool.query -> versiones_matriz`.
Después: `server.js -> LegacyMatrixService -> Repository -> PostgreSQL`.

Se preservan 200, 404, 401 y 500, y no se modifica `/api/evaluacion/version-activa` porque su contrato es distinto.

Prueba: `npm test`, abrir Supervisor > Administrador de Matriz y ejecutar en consola el GET de versión activa.
