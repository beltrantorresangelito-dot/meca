# F6.4 — Users/Auth Module completo

## Resultado

Usuarios/Auth sale de `server.js`.

Arquitectura:

```text
server.js
   ↓
UsersRoutes
   ↓
UsersController
   ↓
UsersService
   ↓
UsersRepository
   ↓
PostgreSQL
```

## Endpoints migrados

- POST `/api/auth/login`
- GET `/api/auth/verify`
- POST `/api/auth/cambiar-password`
- GET `/api/usuarios/auditores`
- GET `/api/usuarios/auditores-activos`
- GET `/api/usuarios`
- POST `/api/usuarios`
- DELETE `/api/usuarios/:id`
- GET `/api/usuarios/exportar`
- PUT `/api/usuarios/:id/password`
- GET `/api/usuarios/:id`
- PUT `/api/usuarios/:id`

## Se mantienen inline

- GET `/api/auth/redirect`
- Roles
- rol_pestanas
- pestanas_sistema

## Seguridad preservada

La validación Bearer central y `authorizeRequest()` siguen ejecutándose
en `server.js` antes del dispatcher del módulo.

## server.js

Antes: 3502
Después: 3129
Reducción: 373

## Próxima fase

F6.5 — Roles/Permisos Repository + Service + módulo progresivo.
